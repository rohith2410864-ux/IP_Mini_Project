# Server Port (IMPORTANT for Render)
server.port=${PORT:8081}

# MongoDB (use environment variable)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/faculty_db}

# Application Name
spring.application.name=faculty-service